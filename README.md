# Library-Circulation-
This SQL script sets up a library management database by creating several connected tables that organize users, books, genres, checkouts, and staff information. It first removes any existing tables to start fresh, then defines new ones with relationships using foreign keys.
